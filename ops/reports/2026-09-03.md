# Keeper shift — 2026-09-03

## Site status: unverified (egress blocked, day 10)

Same block as every shift since 08-25, but worth flagging as its own
issue now rather than folding into the daily routine note: this is a
confirmed organization-level egress **policy denial**, not a flaky
connection. `$HTTPS_PROXY/__agentproxy/status` shows
`recentRelayFailures` with `"kind": "connect_rejected", "detail":
"gateway answered 403 to CONNECT (policy denial or upstream failure)",
"host": "dawnpipe.com:443"`. Per the proxy's own guidance, 403s are not
to be retried or routed around from inside the session — this needs an
allowlist change to the environment's egress policy (or a different
production monitoring path) to ever be checkable from here again. Ten
days of "can't check" is long enough that this is now itself the thing
needing attention, separate from the site's actual health.

## Sanity: clean

`node --check server.js` and every file in `lib/` — all pass. No new
commits landed on `main` since `c577195` (08-26), so nothing new to
sanity-check either.

## Shipped today

**`keeper/2026-09-03-billing-idempotency`** — queue item 12's first
bullet. `/voice/status`'s live-call billing branch in `server.js` now
stamps a `billedMin: true` flag on the call's persisted row the one time
it actually bills the plan's minute allowance, and checks that flag
before billing on every callback. A duplicate delivery of the same
completed status callback (not seen live today, but casual to trigger
later via a retrying proxy or Twilio's `StatusCallbackEvent` config)
previously would have billed the same call's minutes twice; now it's a
no-op. Deliberately left the restart-recovery fallback path (the branch
just above, taken when a deploy wipes the in-memory call mid-call) alone
— that's exactly what pending branch
`audit/2026-08-26-voice-status-fallback-billing` (needs-Alijah item 1
below) already rewrites, and touching those same lines here would hand
you a guaranteed merge conflict between two unmerged branches instead of
none. `node --check` clean; `node test-reps.js` (37/37) and
`node test-spam.js` (18/18) both still pass; smoke-tested the guard
itself against an isolated `DATA_DIR` (first callback bills and stamps,
a simulated duplicate for the same call sees the stamp and skips
billing, an unrelated call is unaffected).

Side finding while scoping this: re-checked
`audit/2026-08-26-voice-status-fallback-billing`'s mergeability with an
actual `git merge --no-commit --no-ff` against current `main` in an
isolated worktree, not just a branch-vs-main diff. The raw diff is
misleading on its own — it makes the branch look like it would revert
`c577195` (08-26's "make a dead mailer visible" password-reset fix),
because the audit branch was cut from the commit just *before* `c577195`
landed and never rebased. The real 3-way merge auto-resolves with no
conflict and both fixes land intact. Confirms (via a different method
than the prior daily `git merge-tree` checks) that this branch is
genuinely safe to merge as-is.

## Needs Alijah — in priority order

1. **The 8 unmerged `keeper/*` branches plus the audit branch are now the
   actual bottleneck, not remaining Keeper work.** Oldest is
   `keeper/2026-08-25-atomic-rep-delete` at 10 days; the live bug it fixes
   (`lib/reps.js`'s raw `writeFileSync` on the commission ledger,
   bypassing the atomic write path) is still on `main` today. Second is
   `audit/2026-08-26-voice-status-fallback-billing` at 8 days — this one
   is actively costing customers money every time a deploy lands mid-call
   (robocalls get billed in full instead of correctly for free). Both are
   re-verified mergeable today, the second one two independent ways. Full
   branch list: `2026-08-25` atomic-rep-delete, `2026-08-27`
   ConversationRelay design, `2026-08-28` activation-funnel + COST.md
   sync, `2026-08-29` Media Streams design, `2026-08-30`
   prune-reset-tokens, `2026-08-31` reconcile-stale-ops-docs, `2026-09-02`
   db-write-compact, and today's billing-idempotency. Suggested merge
   order (bug fixes first, designs and docs are zero-risk any time):
   ```
   git fetch origin
   git checkout main && git pull
   git merge --no-ff origin/audit/2026-08-26-voice-status-fallback-billing
   git merge --no-ff origin/keeper/2026-08-25-atomic-rep-delete
   git merge --no-ff origin/keeper/2026-08-30-prune-reset-tokens
   git merge --no-ff origin/keeper/2026-09-02-db-write-compact
   git merge --no-ff origin/keeper/2026-09-03-billing-idempotency
   git merge --no-ff origin/keeper/2026-08-31-reconcile-stale-ops-docs
   git merge --no-ff origin/keeper/2026-08-28-activation-funnel
   git merge --no-ff origin/keeper/2026-08-29-media-streams-design
   git merge --no-ff origin/keeper/2026-08-27-conversationrelay-design
   git push origin main
   ```
   (each step is independent — merge only the ones you're comfortable
   with, in any order; none conflict with each other as of today).

2. **Session egress to `dawnpipe.com` has been a hard policy 403 for 10
   days**, confirmed via the proxy's own status endpoint, not a transient
   block. If daily live health/latency checks and `ops/seo-check.js`
   (queue item 10) matter to you, this needs an egress-allowlist change on
   the environment config — nothing to fix from inside the session.

3. **`keeper-audit` — is it a standing job?** Still unconfirmed from
   08-26/09-02's ask. Its branch keeps carrying real findings (this
   shift's whole improvement today came from one of them) but isn't
   tracked anywhere this shift would see it land. A one-line answer
   either way avoids this shift rediscovering its output late again.

No customer-facing changes proposed today (email verification and the
2-question onboarding flow, queue items 2 and 8, remain open and still
need a product decision from you before implementation).
