# Keeper Report — 2026-09-12

## Site status
Unverifiable — same gateway-level 403 on CONNECT to `dawnpipe.com:443` for both
`/health` and `/` (`curl` reports `connect_rejected`, "organization policy",
before any request reaches the internet). This is a session egress policy
block, not a site error — 19 days running since first hit on 2026-08-25,
unchanged in character from every prior shift. Cannot confirm the site is up;
cannot confirm it's down either.

## What ran
- Sanity: fresh `main` checkout (`c577195`, still 0 new commits since 08-26 —
  18 days). `node --check` clean on `server.js` + all `lib/*.js` +
  `ops/seo-check.js`. All four suites pass: `test-reps.js` 37/37,
  `test-spam.js` 18/18, `test-voiceprofiles.js` 17/17, `test-assistant.js`
  15/15 (87/87 total).
- Checked for new work: no new commits on `main`, no new branches beyond the
  17 already tracked (12 `keeper/*`, 5 `audit/*`, plus unrelated
  `hiring-screener`/`research-competitors`) — confirmed via `git ls-remote
  origin`. Nothing new to audit or ship. Did not re-verify the standing
  branches' mergeability again today — already re-confirmed against this
  exact `main` commit repeatedly this week; doing it again would be motion,
  not new information.
- Not Friday — no COST.md re-read today.

No `keeper/` branch shipped today — every code-workable queue item is already
shipped, closed, correctly held as a customer-facing proposal, or waiting
purely on a merge, and the underlying repo hasn't moved in over two and a
half weeks.

## Needs Alijah (top 3)
1. **Merge backlog, unchanged: 12 `keeper/*` branches + 5 `audit/*` branches,
   oldest (`keeper/2026-08-25-atomic-rep-delete`) now 19 days old.** Four are
   live bugs: two billing/metering-correctness
   (`audit/2026-08-26-voice-status-fallback-billing`,
   `audit/2026-09-02-consume-stale-snapshot`) and two security/availability
   (`audit/2026-09-09-spam-strike-decay`,
   `audit/2026-09-09-voice-webhook-log-flood`). All re-verified clean against
   current `main` this week. Full list and context in
   `ops/IMPROVEMENT-QUEUE.md`. This backlog, not remaining Keeper work, is the
   actual bottleneck — nothing here has changed in over a week of daily
   flagging.
2. **19 days of blocked session egress to `dawnpipe.com`.** Blocks the daily
   health check and `ops/seo-check.js` (present on `main` since 08-25, never
   run by this shift). Worth checking whatever changed in the environment's
   egress allowlist around 08-25 if dawnpipe.com is meant to be reachable
   from here — this has now outlasted every other open item.
3. **Weekly reminder (COST.md item 2, last raised 08-28): check which
   speech-recognition model Twilio is billing you for.** `/api/admin/spend` →
   `diagnostics` (owner login). If it shows the legacy STT model, set
   `VOICE_STT=default` in Render's env — one env var, ~$45–60/1,000 min
   savings. Only worth checking if not already confirmed.
