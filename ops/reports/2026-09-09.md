# Keeper Report — 2026-09-09

## Site status
Unverifiable — same gateway-level 403 on CONNECT to `dawnpipe.com:443`. Confirmed
three ways today: direct `curl`, `WebFetch`, and the proxy status endpoint
(`recentRelayFailures` shows two `connect_rejected` entries, "gateway answered 403
to CONNECT (policy denial or upstream failure)"). This is a session egress policy
block, not a site error — **18 days running now** (since 2026-08-25/26). Still the
longest-standing open item in this queue.

## What ran
- Sanity: `node --check` clean on `server.js`, all `lib/*.js`, and
  `ops/seo-check.js` (fresh `main` checkout, commit `c577195`). All four test
  suites pass: `test-reps.js` 37/37, `test-spam.js` 18/18, `test-voiceprofiles.js`
  17/17, `test-assistant.js` 15/15 (87/87 total).
- `git rev-list origin/main ^c577195 --count` = 0 — no new commits on `main`
  since 2026-08-26 (18 days). Nothing new to audit, nothing new to ship from
  that angle.
- Did not re-run mergeability checks on the standing `keeper/*`/`audit/*`
  branches again today — every one has already been re-verified against this
  exact `main` commit multiple times this week (most recently 09-08); doing it
  again would be motion, not new information, per the same call made
  09-01/09-03/09-07/09-08.
- Instead, closed out **queue item 8** (onboarding: "2 questions → AI drafts
  profile → instant first leads"), which had sat as an unstarted proposal
  placeholder since the queue was first seeded. Traced the actual code
  (`server.js`, `lib/agents.js`, `views/app.html`) and found it's
  **substantially already shipped**, predating this queue entirely:
  `POST /api/profile/autofill` (`agents.autofillFromWebsite`) takes one pasted
  URL — or raw text for sites it can't fetch (JS-only pages, WAFs blocking
  datacenter IPs, sites that are down) — and drafts the whole profile (offer,
  ICP, value prop, proof, tone, CTA) *plus a full phone script* in one AI call.
  It's wired to a real "✨ Fill it in for me" button in the profile-setup UI,
  and a locked (no-plan) account gets 2 free autofills specifically so this
  works before any card is entered. That's the "2 questions" idea delivered as
  something simpler (one URL, not two prompts) and richer (a phone script
  too). One real gap: leads aren't fully instant — after autofill+save the
  owner still clicks a separate "🎯 Find leads" button. Left that gap as a
  proposal in the queue rather than building it (see below) since it's a
  customer-facing behavior change.
- No new unrelated branches noticed today beyond the already-known
  `hiring-screener` and `research-competitors`.

No branch shipped today — this was a docs-only shift (`ops/IMPROVEMENT-QUEUE.md`,
`ops/KEEPER-LOG.md` updated on `keeper-ops`).

## Proposal (customer-facing — needs sign-off before building)
Auto-fire one small first prospect run (~5 leads, no auto-draft/send) the
moment a profile is saved for the first time *from* an autofill, so a
brand-new account sees its first leads without the extra "Find leads" click.
Scoped to only the first save-after-autofill transition, so it can never
repeat or surprise a returning user. Not built — flagging for a decision.

## Needs Alijah (top 3, unchanged from recent days)
1. **Merge backlog — 14 `keeper/*` branches + 2 `audit/*` branches, oldest 18
   days old.** Two are live billing/metering-correctness bugs actively
   costing money every day they sit unmerged:
   - `audit/2026-08-26-voice-status-fallback-billing` — overbills robocalls/
     dead-air/pocket-dial calls when a deploy lands mid-call (routine, given
     daily deploys).
   - `audit/2026-09-02-consume-stale-snapshot` — concurrent night-shift jobs
     silently erase most of a run's metered leads/voice minutes (repro: 21
     metered, 1 recorded). Under-meters, i.e. costs Dawnpipe AI spend that
     never draws down a customer's plan/balance.
   Merge command for each (safe to run in any order, all independently
   re-verified clean against current `main` as recently as this week):
   `git checkout main && git pull && git merge --no-ff audit/2026-08-26-voice-status-fallback-billing`
   then repeat for the other branches. Full list in `ops/IMPROVEMENT-QUEUE.md`.
2. **Session egress to `dawnpipe.com` has been blocked 18 days straight.**
   Beyond blocking the daily health check, it also means `ops/seo-check.js`
   (a real SEO/trust-signal monitor already on `main`) hasn't run once since
   it was added on 2026-08-25. Worth checking whatever changed in the
   sandbox's egress policy around that date, if the site is meant to be
   reachable from here.
3. **Sign off (or not) on the small onboarding proposal above** — auto-firing
   one first prospect run right after a first autofill, so new accounts see
   leads without an extra click.

No Friday COST.md re-read today (Tuesday).
