# Dawnpipe Keeper report — 2026-09-18

## Site status

Health unverifiable from this session: `https://dawnpipe.com/health` and `/` both return
connection failures — the egress proxy rejects the CONNECT to `dawnpipe.com:443` with a
`403` (`connect_rejected`, "organization policy"), confirmed with a raw `curl -v` against
the proxy. Same cause pinned exactly on 2026-09-15 (`x-deny-reason: host_not_allowed`): this
session's environment network policy does not have `dawnpipe.com` on its egress allowlist.
25th consecutive day (first hit 2026-08-25). This is a session/environment config gap, not a
report of the site being down — no evidence either way on real uptime or latency today.

**Fix (unchanged ask):** add `dawnpipe.com` to this Keeper environment's egress allowlist so
the daily health check and `ops/seo-check.js` can actually run.

## Sanity

`node --check` clean on `server.js` and all files in `lib/` (fresh `main`, `a70c1ec`). All
five suites pass, 96/96 (37 reps + 18 spam + 17 voiceprofiles + 15 assistant + 9
sending-mode). No new commits on `main` since 2026-09-16 (`a70c1ec`, 2 days). No new branches
beyond the 21 already tracked (14 `keeper/*` + 7 `audit/*`, confirmed via `git ls-remote
origin`).

## Shipped today

Nothing. No new commits landed on `main` to audit, and no new independent audit branch
appeared since 2026-09-16. Every code-workable queue item is already shipped, closed, held
as a customer-facing proposal, or waiting purely on a merge. Did not re-run mergeability
checks on the standing 21 branches again — they were independently re-verified against this
exact `main` commit on both 2026-09-16 (full backlog) and 2026-09-17 (items 21/22
specifically); re-checking a third time against an unchanged `main` would be motion without
new information, the same pattern flagged repeatedly in `ops/KEEPER-LOG.md`.

**Friday COST.md check.** Re-read `COST.md` in full against current `main` — no
discrepancies found, figures and architecture comparison (A/B/C/D) still match the shipped
code (Sonnet on voice turns, prompt caching wired, per-account SMTP with zero pooled sending
cost except the new `useDawnpipe` path). Nothing to correct. Repeating the standing reminder
below since it's been a week since the last one (2026-09-11) and it's still unconfirmed.

## Needing Alijah (top 3)

1. **Merge `audit/2026-09-16-smtp-crlf-injection` now — still the single highest-priority
   item in the backlog, unmerged 2 days.** Critical SMTP injection reachable by any account
   with trial-level access; on "let Dawnpipe send for you" accounts it fires through
   Dawnpipe's own system mailbox. Re-verified clean against current `main` as recently as
   yesterday.
   `git fetch origin && git checkout main && git merge --no-ff origin/audit/2026-09-16-smtp-crlf-injection && git push origin main`

2. **Check `/api/admin/spend` → `diagnostics` for the STT model billed** (owner login,
   ~2 minutes). If it shows a legacy speech-recognition model instead of
   `experimental_conversations`, set `VOICE_STT=default` in Render's environment — worth
   $45–60 per 1,000 minutes. Standing weekly reminder per `COST.md`; last raised 2026-09-11.

3. **Add `dawnpipe.com` to this environment's egress allowlist.** 25 days blind on
   uptime/latency now — this shift cannot see a real outage even if one happens.

Standing backlog otherwise unchanged: 14 `keeper/*` + 7 `audit/*` branches (21 total), oldest
24 days old, three of them live billing/security/availability bugs (items 11, 14, and the
CRLF injection above) — the backlog itself, not remaining Keeper work, is still the
bottleneck (see item 23 in `ops/IMPROVEMENT-QUEUE.md`).
