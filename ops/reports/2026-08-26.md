# Dawnpipe Keeper — 2026-08-26

## Site health
Could not check. This session's outbound network proxy rejected the CONNECT
to `dawnpipe.com:443` with a 403 ("policy denial or upstream failure")
before any request reached the site — the second day running this
session's network policy has blocked egress to the site entirely. Not
reporting SITE DOWN; just unverified two days in a row. Worth checking
whether the environment's egress allowlist needs `dawnpipe.com` added if
daily health checks matter.

## Sanity checks
`node --check` passed on `server.js` and every file in `lib/`. Clean.

## Found: two divergent `ops/` states (main vs. `keeper-ops`)
`main` carries its own `ops/IMPROVEMENT-QUEUE.md` and `ops/KEEPER-LOG.md`,
introduced directly on main in commit `ad5e278` ("Per-rep commission
rates...") — bundled into a commit whose message is about something else
entirely. That queue is structured differently (10 items grouped by
Reliability/Cost/Growth, referencing `AUDIT.md`) from the `keeper-ops`
branch's queue (8 items, seeded per this run's own instructions), and its
`KEEPER-LOG.md` has one baseline entry from 2026-08-25 11:21am with a real
health check (200s, 0.41s/0.17s) — meaning something with real network
access ran a keeper-like process directly against main that day, separate
from the actual `keeper-ops` shift later that day. I have not touched
main's copy (hard rule: never push to main) and kept working the
`keeper-ops` queue as instructed. Flagging because these will keep
diverging silently otherwise, and it's not obvious to me which is meant to
be authoritative.

## Shipped
No `keeper/*` branch today — every finding below was a documentation/queue
correction, not a code change, so it's committed straight to `keeper-ops`.

- **Queue item 1** (db.js write safety): still open — `keeper/2026-08-25-atomic-rep-delete`
  hasn't been merged yet. Re-verified it still merges cleanly against
  today's main (no conflicts) and re-ran the write-bypass audit against
  everything main gained since (spam filter, assistant, Stripe, per-profile
  voice, commissions) — no new bypasses found.
- **Queue item 3** (stuck-call/webhook audit): audited every other
  stateful in-flight tracker in the codebase (background job maps, queue
  items, appointments) for the same failure shape as the fixed stuck-calls
  bug. Found none — the existing fix already covers inbound and outbound
  calls, and everything else either isn't persisted or can't get stuck in
  a transient state. Recommend closing this queue item.
- **Queue item 4** (prompt-caching on live-call turns): already fully
  implemented since 2026-08-17 (`cache_control` on the system block, a
  cache-warming call before the first turn, 16-turn history cap). `COST.md`
  still listed it as an open lever and cited a Haiku per-turn cost that's
  been stale since voice turns moved to Sonnet on 2026-08-24 (deliberate
  quality tradeoff, in `695a9aa`). Corrected `COST.md` and closed the queue
  item; flagged that the new per-turn AI cost hasn't been re-measured.

Full detail in `ops/IMPROVEMENT-QUEUE.md`.

## Needs Alijah
1. **Reconcile the two `ops/` queues** (main vs. `keeper-ops` — see above).
   Decide which is authoritative, or fold one into the other. Not urgent,
   but will get more confusing the longer both are edited independently.
2. Merge `keeper/2026-08-25-atomic-rep-delete` into main when convenient —
   still small, single-purpose, still merges clean, tests still pass.
3. If daily `dawnpipe.com` health checks from this session matter, the
   environment's egress policy is blocking them (403 on CONNECT, both
   today and yesterday) — may need `dawnpipe.com` added to whatever
   allowlist governs this session's outbound network.

Not a Friday — no COST.md/spend admin-check reminder due today (the
COST.md edit above was a correction found during unrelated auditing, not
the weekly spend-check ritual).
