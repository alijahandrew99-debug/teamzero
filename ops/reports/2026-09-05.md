# Dawnpipe Keeper report — 2026-09-05

## Site status

**Unverifiable, not down.** Direct `curl` to `https://dawnpipe.com/health` and
`/` both failed with `HTTP:000` in ~0.2s — the agent proxy rejected the CONNECT
with a 403 (`curl -v` shows the tunnel itself getting `HTTP/1.1 403 Forbidden`
from the gateway, and `curl -sS "$HTTPS_PROXY/__agentproxy/status"` confirms two
fresh `recentRelayFailures` entries: `connect_rejected`, "gateway answered 403
to CONNECT (policy denial or upstream failure)", host `dawnpipe.com:443`).
Same org-level egress policy block flagged every day since 2026-08-25, now
**12 days running**. No evidence of an actual outage — just no way to check
from this session's network policy. `ops/seo-check.js` not re-run (same
block, last tried 2026-08-31).

## Sanity

`node --check server.js` and all 28 files in `lib/` — clean.
All four suites pass: `test-reps.js` 37/37, `test-spam.js` 18/18,
`test-voiceprofiles.js` 17/17, `test-assistant.js` 15/15 (87/87 total).

No new commits landed on `main` since 2026-08-26 (`c577195` still the tip),
so nothing new to audit there and yesterday's mergeability checks on the
standing branches stand unchanged.

## Shipped

**`keeper/2026-09-05-per-account-retention`** — queue item 15. `lib/db.js`'s
`saveCall` and `saveThread` each capped their collection with a global
`slice(-N)` (`calls.json` at 3000, `sms_threads.json` at 2000) — the exact bug
already fixed for `activity.json` (`capPerAccount`, with a comment explaining
why), never carried across. Worse for `smsThreads`: it's live in-flight state
the missed-call text-back flow reads for context, and eviction was by
creation order, not recency, so a thread texted all day today could still be
evicted before an untouched one from last week. Added
`capPerAccountOldestFirst()` — same idea as the existing `capPerAccount` but
walks backward (since these two collections push/oldest-first instead of
unshift/newest-first) to keep the newest N per account, then restores
oldest-first order. Applied to both writes together, as the 2026-09-02 audit
that found this asked. `node --check` clean; `test-reps`/`test-spam`
unaffected (neither exercises retention at this scale); a 6-assertion smoke
test against three isolated `DATA_DIR`s confirms a quiet tenant's one
call/thread survives 600 writes from a busy tenant on the same file, and a
row created early but updated 600-writes-later survives the cap. Cut fresh
off `main` (unchanged since 08-26), so no conflict risk with the in-flight
`keeper/2026-09-02-db-write-compact`/`keeper/2026-09-04-unique-tmp-write`
branches that also touch `lib/db.js` but haven't merged.

Also noticed while merging `main` into `keeper-ops` for this shift's own
docs: it conflicted (add/add) on `IMPROVEMENT-QUEUE.md`/`KEEPER-LOG.md`
again, same root cause flagged 2026-08-26 through 2026-08-30 and supposedly
fixed 2026-08-31 (`keeper/2026-08-31-reconcile-stale-ops-docs`) — that fix
branch itself is still unmerged, so `main` still carries the stale duplicate
from `ad5e278`, and this conflict will keep recurring on every shift until
that branch lands. Resolved locally by keeping `keeper-ops`'s own docs;
no data lost, but it's now costing a few minutes of every shift, not just
sitting as clutter.

## The three things needing you

1. **Merge backlog, now 13 small branches deep — the actual bottleneck.**
   Two are billing/metering-correctness fixes actively costing money or
   under-counting usage every day they sit unmerged:
   - `audit/2026-08-26-voice-status-fallback-billing` — a deploy-mid-call
     robocall/pocket-dial gets billed when it should be free (13 days
     unmerged, still re-verified clean as of 09-04).
   - `audit/2026-09-02-consume-stale-snapshot` — the night shift's
     concurrent jobs silently erase most of a run's metered leads/voice
     minutes (reproduced 21-metered-1-recorded).
   Locally: `git fetch origin && git checkout main && git merge --no-ff
   origin/<branch-name>` for each, then push. Full list with descriptions
   in `ops/IMPROVEMENT-QUEUE.md`. Suggest starting with
   `keeper/2026-08-31-reconcile-stale-ops-docs` specifically — it's small,
   docs-only, and merging it stops this shift's own merge conflict from
   recurring daily.

2. **Ten-minute Render dashboard check (repeat ask, not urgent).** Does
   this service's deploy strategy ever run the old and new instance
   concurrently against the same persistent disk? Render dashboard → this
   service → Settings → deploy/zero-downtime behavior, or deploy history
   for overlapping "live" windows. `keeper/2026-09-04-unique-tmp-write`
   already defends against this regardless — just tells you whether it was
   live or hypothetical.

3. **Confirm the egress policy is intentional.** 12 days running now on a
   block that reads as a deliberate org policy denial (403 straight from
   the gateway, not a timeout) rather than a transient issue — worth a
   yes/no on whether `dawnpipe.com` should be reachable from this session
   so future reports can stop repeating this.

Full detail on every queue item's status: `ops/IMPROVEMENT-QUEUE.md`.
