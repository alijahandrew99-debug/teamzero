# Dawnpipe Keeper — 2026-08-28

## Site health
Could not check. This session's outbound network proxy rejected the CONNECT
to `dawnpipe.com:443` with a 403 ("policy denial or upstream failure") before
any request reached the site — confirmed a genuine org-policy denial via the
proxy's own status endpoint (`recentRelayFailures`), not a flaky timeout.
Fourth day running this session's network policy has blocked egress to the
site entirely. Not reporting SITE DOWN; just unverified four days in a row.
See "Needs Alijah" below — this is worth fixing rather than re-flagging daily.

## Sanity checks
`node --check` passed on `server.js` and every file in `lib/`. Clean.
`node test-reps.js` — 37 passed, 0 failed (re-ran after the funnel change).

## Shipped this shift

**Branch `keeper/2026-08-28-activation-funnel`** — queue item 7, activation
analytics. Tracks the funnel signup -> profile -> first lead -> first send ->
upgrade into a new flat `funnel.json`, one row per account per milestone
(first crossing only — no per-event bloat, no third-party tracker). Hooked
at `db.createAccount`, the profile-save route, `db.addLeads`, `db.logSend`,
and the Stripe `checkout.session.completed` handler. Added owner-only
`GET /api/admin/funnel` for the counts. Not customer-facing. Smoke-tested
against an isolated data dir (each milestone counts once, duplicates and
garbage step names are safe no-ops) and the full test suite still passes.

**Branch `keeper/2026-08-28-cost-md-sync`** — small doc-only fix, not a
queue item. The 2026-08-26 shift corrected `COST.md`'s prompt-caching
section but committed it straight to the `keeper-ops` state branch instead
of a `keeper/*` branch, so it's been silently diverging from `main`'s copy
for two days without ever landing. Re-verified both underlying facts against
today's code (prompt caching, cache-warming and the 16-turn history cap are
all still in place; voice turns are still on Sonnet) and ported the same
9-line correction to `main`'s `COST.md`.

## Friday: COST.md re-read
Re-read `COST.md` in full. Nothing new to add beyond the sync above — the
two-day-old drift was the only staleness found. As a standing reminder (at
most once a week, per your own doc): pull `GET /api/admin/spend` ->
`diagnostics` and check which speech-recognition model is billed. If it's
a legacy model (~$0.035-0.040/use) rather than the $0.02 default, set
`VOICE_STT=default` in Render — worth ~$45-60 per 1,000 minutes for a one
env var change. Also worth a look while you're in there: turns/min and
chars/turn, both explained in `COST.md`'s "Open questions" section.

## Also noted, not actioned
- **Queue item 1** (db.js write safety): `keeper/2026-08-25-atomic-rep-delete`
  is still open, unmerged, three days now. Still merges clean, tests still
  pass, nothing further for the keeper to do until it lands.
- **Queue item 2** (email verification): still open, customer-facing —
  proposed gating options are in `ops/reports/2026-08-27.md`, awaiting your
  call.
- **Queue item 6** (Media Streams + Deepgram + Polly-direct design, arch D):
  confirmed still open, deliberately not started. It's a strict superset of
  the arch-C design work already done (item 5) plus two new vendor
  integrations (Deepgram STT, Polly TTS) and raw audio-frame handling —
  not a one-shift design doc. Recommend picking it up only after arch C's
  half-day spike (per item 5) tells us whether ConversationRelay is even the
  right next step, since arch D inherits everything unresolved there.
- The two divergent `ops/` states (`main`'s own copy vs. `keeper-ops`'s),
  flagged 2026-08-26, are unchanged — still not reconciled.

## Needs Alijah
1. **Network policy**: this session's egress proxy has blocked
   `dawnpipe.com` four shifts running (403 on CONNECT). If daily health
   checks matter, `dawnpipe.com` needs adding to whatever egress allowlist
   governs this session — otherwise step 1 of every future shift is dead
   weight. Diagnostic used: `curl -sS $HTTPS_PROXY/__agentproxy/status`.
2. Merge `keeper/2026-08-25-atomic-rep-delete` when convenient (still small,
   single-purpose, still merges clean, tests pass) — three days open now.
3. Friday spend check: `/api/admin/spend` -> `diagnostics`, confirm
   `VOICE_STT` isn't billing the legacy speech model (see above).

Two branches to review/merge this shift: `keeper/2026-08-28-activation-funnel`
and `keeper/2026-08-28-cost-md-sync`.
