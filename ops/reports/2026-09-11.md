# Keeper Report — 2026-09-11

## Site status
Unverifiable — same gateway-level 403 on CONNECT to `dawnpipe.com:443`, confirmed
via direct `curl` (`/health` and `/`, both `connect_rejected`) and the proxy
status endpoint (`recentRelayFailures`: "gateway answered 403 to CONNECT
(policy denial or upstream failure)", timestamped today). This is a session
egress policy block, not a site error — 18 days running since first hit on
2026-08-25, unchanged in character from every prior shift. Cannot confirm the
site is actually up; cannot confirm it's down either. Longest-standing open
item, now past two and a half weeks.

## What ran
- Sanity: fresh `main` checkout (`c577195`, still 0 new commits since 08-26 —
  18 days). `node --check` clean on `server.js` + all `lib/*.js` +
  `ops/seo-check.js`. All four suites pass: `test-reps.js` 37/37,
  `test-spam.js` 18/18, `test-voiceprofiles.js` 17/17, `test-assistant.js`
  15/15 (87/87 total).
- Checked for new work: no new commits on `main`, no new branches beyond the
  17 already tracked (12 `keeper/*`, 5 `audit/*`, plus unrelated
  `hiring-screener`/`research-competitors`). Nothing new to audit or ship.
  Did not re-verify the standing branches' mergeability again today — already
  re-confirmed against this exact `main` commit repeatedly this week; doing
  it again would be motion, not new information.
- Friday COST.md re-read: read it in full against current code. No
  discrepancies found — the prompt-caching line (queue item 4) and the
  transfer-leg-cost gap (item 12) are the only two things this doc used to
  get wrong, and both are already corrected/shipped. Nothing new to correct.

No `keeper/` branch shipped today — every code-workable queue item is already
shipped, closed, correctly held as a customer-facing proposal, or waiting
purely on a merge, and the underlying repo hasn't moved in over two weeks.

## Needs Alijah (top 3)
1. **Merge backlog, unchanged: 12 `keeper/*` branches + 5 `audit/*` branches,
   oldest (`keeper/2026-08-25-atomic-rep-delete`) now 18 days old.** Two of
   the audit branches are live billing/metering-correctness bugs
   (`audit/2026-08-26-voice-status-fallback-billing`,
   `audit/2026-09-02-consume-stale-snapshot`); two more are a live
   security/availability pair from 09-09
   (`audit/2026-09-09-spam-strike-decay`,
   `audit/2026-09-09-voice-webhook-log-flood`). All re-verified clean against
   current `main` as of this week. Full list and merge commands in
   `ops/IMPROVEMENT-QUEUE.md`. This backlog, not remaining Keeper work, is
   the actual bottleneck.
2. **Weekly reminder (Friday, COST.md item 2): check which speech-recognition
   model Twilio is billing you for.** `/api/admin/spend` → `diagnostics`
   (owner login) says which. If it shows the legacy model, set
   `VOICE_STT=default` in Render's env — one env var, ~$45–60/1,000 min.
   Only worth checking if this hasn't been confirmed already; last explicit
   reminder was 2026-08-28.
3. **18 days of blocked session egress to `dawnpipe.com`.** Blocks the daily
   health check and `ops/seo-check.js` (on `main` since 08-25, never run).
   Worth a look at whatever changed in the sandbox's egress policy around
   08-25 if the site is meant to be reachable from here.
