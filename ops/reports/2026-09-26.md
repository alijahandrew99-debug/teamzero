# Keeper report — 2026-09-26

**Site health: unverifiable this session** (same gateway-level rejection on
CONNECT to `dawnpipe.com:443`, `connect_rejected` / "organization policy" —
33rd consecutive day since first hit 08-25. Unchanged root cause: this
session's environment egress allowlist doesn't include `dawnpipe.com`. Not
evidence of an outage — just can't be checked from here. Per the proxy's own
guidance (`/root/.ccr/README.md`), this class of denial is not retried or
routed around.

**Sanity: clean.** `node --check` on `server.js` and all `lib/*.js` files
against current `main` (`facb991`, unchanged since yesterday).

## What happened today

Nothing new to audit or ship. `main` is still at `facb991` — the one commit
that landed yesterday (`plans.fromPrice()` on the signup page) was already
read and cleared in the 09-25 report. No new commits since.

Checked the independent `keeper-audit` process for a new run: still at
`cbe42f0` (2026-09-23, lens (a)), unchanged since yesterday — no new
findings to fold in.

The merge backlog (23 branches: 14 `keeper/*` + 9 `audit/*`) was
re-verified against this exact `main` commit yesterday via real
`git merge --no-commit --no-ff` (all four top-priority branches together,
suites passing on the merged tree). Re-running that same check today against
an unchanged base and an unchanged branch set would be pure repeated
overhead with no new information, so it was skipped — consistent with how
the last few quiet days (09-21 through 09-23) handled the same situation.

No `keeper/*` branch shipped today. Not Friday, so no `COST.md` re-read this
shift.

## Needs Alijah (≤3, exact actions)

1. **Merge backlog is still the whole bottleneck — 23 branches, zero
   merged, unchanged since yesterday.** Top four, verified clean as of
   yesterday against current `main` (`facb991`):
   - `audit/2026-09-23-send-lock-duplicate` — High: a second click on a send
     job past ~6 emails force-expires it and double-sends the remainder,
     live since 09-15.
   - `audit/2026-09-23-readbody-utf8` — High: multi-byte UTF-8 split across
     a TCP chunk can corrupt the Stripe webhook signature check, silently
     dropping a genuine payment event.
   - `audit/2026-09-16-smtp-crlf-injection` — Critical: unvalidated lead
     emails let a CRLF forge SMTP commands through Dawnpipe's own sending
     mailbox.
   - `audit/2026-09-16-esc-quotes` — Medium: unescaped quotes in an HTML
     attribute, one path crosses into the owner's own admin session.
   One-shot merge command (locally, then push):
   `git fetch origin && git checkout main && git pull && git merge --no-ff origin/audit/2026-09-23-send-lock-duplicate origin/audit/2026-09-23-readbody-utf8 origin/audit/2026-09-16-smtp-crlf-injection origin/audit/2026-09-16-esc-quotes`
   This has been the top ask for two weeks and is costing real money and
   customer trust every day it sits unmerged.
2. **This session's network policy still blocks `dawnpipe.com` egress** — a
   33-day-old, purely environmental issue (not a site problem), still
   preventing direct health/SEO-monitor checks (`ops/seo-check.js`). Same
   standing question as before: worth fixing on the environment side, or
   accepted as a permanent limitation of this shift. Not re-raising details
   daily going forward unless something about it changes — flagging here
   once more since it's been two weeks since the last mention of the
   decision itself.
