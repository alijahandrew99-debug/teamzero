# Dawnpipe Keeper — 2026-08-25

## Site health
Could not check. This session's outbound network proxy rejected the CONNECT
to dawnpipe.com with a 403 ("policy denial or upstream failure") before any
request reached the site — a restriction on this session's network policy,
not evidence the site is down. Not reporting SITE DOWN; just unverified
today.

## Sanity checks
`node --check` passed on server.js and all 26 files in lib/. Clean.

## GitHub access
Denied for the first part of the shift — both `git push` and the GitHub MCP
tools returned 403 ("Resource not accessible by integration"), a
connector/App-install issue, not a code problem. Alijah fixed it mid-shift;
confirmed working and the branch below pushed successfully.

## Shipped
Branch `keeper/2026-08-25-atomic-rep-delete` (commit baa5995), off latest main:

- `lib/reps.js` `deleteRep()` was rewriting `reps.json` — the commission
  ledger's rep table — with a raw `fs.writeFileSync`, bypassing the
  temp-file+rename atomic write every other mutation in `db.js` uses. A crash
  mid-write (deploy restart, OOM) could have truncated/corrupted that file.
- Added `db.deleteCollection(name, id)` in `lib/db.js`, routed through the
  existing atomic `write()`, and pointed `deleteRep` at it.
- `test-reps.js`: 37/37 pass. `node --check` clean on both files.
- Addresses part of queue item 1 (db.js write safety) — see
  `ops/IMPROVEMENT-QUEUE.md` for the full status and scoping note on the
  remaining "per-file write queue" half of that item.

## Needs Alijah
1. Merge `keeper/2026-08-25-atomic-rep-delete` into main when convenient —
   small, single-purpose, tests pass.
2. No action needed on today's earlier GitHub-access outage — already
   resolved by you mid-shift, noted here only for the record.
3. Nothing else outstanding today (not a Friday — no COST.md/spend check due).
