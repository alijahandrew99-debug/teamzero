# Keeper Report — 2026-09-16

## Site status
Unverifiable — same gateway-level block on `dawnpipe.com` (403 on CONNECT
to port 443, "organization policy", reconfirmed via the proxy status
endpoint's `recentRelayFailures`). 23 days running since first hit
2026-08-25. Cannot confirm the site is up; cannot confirm it's down
either. Still the single longest-standing open item in this queue.

## What ran
- Sanity: fresh `origin/main` checkout. **`main` moved for the first time
  in 22 days** — `c577195` → `a70c1ec`, 6 new commits shipping a one-click
  "let Dawnpipe send for me" mailbox option (routes a customer's outreach
  through the shared system mailbox, no app password required) plus
  MX-based SMTP host auto-detection for custom domains. `node --check`
  clean on `server.js` + all 27 `lib/*.js` files. All five test suites
  pass, 98/98 (37 reps + 18 spam + 17 voiceprofiles + 15 assistant + 12
  sending-mode, the last including 3 new assertions added today).
- Audited the new commits the way this queue audits everything landing on
  `main` (same pattern as items 11/14/17-19's history) and found a real
  gap, described below.
- Shipped `keeper/2026-09-16-dawnpipe-send-warmup` (queue item 20): the
  new `useDawnpipe` one-click connect never started warmup for the shared
  `support@dawnpipe.com` sending domain — unlike the own-mailbox path one
  block below it in the same handler, which explicitly restarts warmup on
  a domain change. A brand-new account had no warmup state at all, and
  `sending.warmupAllowance()` treats that as unlimited, so it could send
  its full daily cap (default 50) through that domain on day one with zero
  pacing. That domain is pooled across every customer who opts into this
  feature — it's the one domain in the whole system that most needs the
  ramp, since one customer blasting their cap on day one degrades
  deliverability for everyone else sharing it. Fix mirrors the
  own-mailbox path's exact restart-on-domain-change logic. `node --check`
  clean; 3 new test assertions (warmup enabled on the Dawnpipe domain,
  day-one allowance is the warmup floor of 10 not unlimited, re-enabling
  on an unchanged domain doesn't reset the ramp); 98/98 across all suites.
- Re-verified all 18 standing `keeper/*`/`audit/*` branches actually merge
  clean against the new `main` — via a real `git merge --no-commit --no-ff`
  in an isolated worktree, not `git merge-tree` (which false-flagged all
  14 non-design branches as conflicting today, the same coarse-tool false
  alarm flagged 2026-09-01). Nothing broke when main moved; all still
  clean. Merge backlog is now **14 `keeper/*` + 5 `audit/*` branches (19
  total)**, oldest 23 days old.
- Not Friday — no COST.md re-read today.

## Needs Alijah (top 3)
1. **Merge backlog: 14 `keeper/*` + 5 `audit/*` branches (19 total),
   oldest (`keeper/2026-08-25-atomic-rep-delete`) now 23 days old.** Five
   touch billing/security/availability correctness directly:
   `audit/2026-08-26-voice-status-fallback-billing`,
   `audit/2026-09-02-consume-stale-snapshot`,
   `audit/2026-09-09-spam-strike-decay`,
   `audit/2026-09-09-voice-webhook-log-flood`, and
   `keeper/2026-09-13-topup-write-race`. All re-verified today as still
   merging clean against current `main`. This isn't remaining Keeper
   work — it's fixes sitting done, tested, and unmerged for weeks. Full
   list and context in `ops/IMPROVEMENT-QUEUE.md`.
2. **23 days of blocked session egress to `dawnpipe.com`.** Confirmed
   again today at the proxy level: `403` on CONNECT, "organization
   policy". This session's environment network policy needs `dawnpipe.com`
   added to its egress allowlist. Blocks the daily health check and
   `ops/seo-check.js`.
3. **Weekly reminder (COST.md item 2, last raised 08-28): check which
   speech-recognition model Twilio is billing you for.** `/api/admin/spend`
   → `diagnostics` (owner login). If it shows the legacy STT model, set
   `VOICE_STT=default` in Render's env — one env var, ~$45–60/1,000 min
   savings. Only worth checking if not already confirmed.
