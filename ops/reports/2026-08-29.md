# Dawnpipe Keeper — 2026-08-29

## Site health
Could not check. This session's outbound network proxy rejected the CONNECT
to `dawnpipe.com:443` with a 403 ("policy denial or upstream failure")
before any request reached the site, for both `/health` and `/`. Fifth day
running this session's network policy has blocked egress to the site
entirely. Not reporting SITE DOWN; just unverified five days in a row. See
"Needs Alijah" below.

## Sanity checks
`node --check` passed on `server.js` and every file in `lib/`. Clean.
`node test-reps.js` — 37 passed, 0 failed.

## Shipped this shift
**Branch `keeper/2026-08-29-media-streams-design`** — queue item 6, the
Media Streams + Deepgram + Polly-direct design (arch D in `COST.md`).
Design doc only, `ops/design/media-streams-deepgram-polly-design.md`.
Grounded in `lib/voice.js` as it exists today and in item 5's already-
written arch-C doc. Covers:
- What D needs beyond C: a hand-rolled WebSocket *server* for Twilio Media
  Streams (Node has no built-in one — feasible with zero dependencies via
  the `crypto` module, but new protocol-level code, not a small lift), a
  WebSocket client to Deepgram (Node 22's built-in `WebSocket` handles
  this trivially), and direct AWS Polly calls with SigV4 signing plus
  reply-chunking for low-latency streamed TTS.
- What ports unchanged from `lib/voice.js` (`think()`, `systemPrompt()`,
  prompt caching, call state) vs. what has to be rebuilt from scratch
  against new vendor semantics (`isNoise()`, recognition hints, barge-in,
  machine detection, recording, WS auth).
- Recommendation: **design only, do not implement or even spike yet** —
  D inherits every open question item 5's arch-C doc raised and adds new
  ones (the WS server, reply chunking), so implementing it before arch C
  itself lands and proves out would mean redoing the shared parts twice.
  Matches `COST.md`'s own ordering ("after C proves out").

## Also verified this shift, not actioned
- **Queue item 1** (db.js write safety): re-audited every file main has
  gained since the last check (password reset, social-share card,
  forwarding divert, SEO/schema work, phone-setup-per-business, website
  import, spam filter, in-app assistant) — no new raw `writeFileSync`
  bypass sites. `keeper/2026-08-25-atomic-rep-delete` still merges clean
  against current main. Still unmerged — **fourth day now**.
- **Queue item 2** (email verification): re-verified still fully open.
  Proposed gating options unchanged, see `ops/reports/2026-08-27.md`.
- The divergent `ops/` states (`main`'s stale copy, seeded 2026-08-25 and
  never updated, vs. `keeper-ops`'s active one) — still unreconciled,
  flagged again for the fourth day. One useful thing in main's stale copy:
  its one log line records a real baseline from 2026-08-25 — `/health` 200
  in 0.41s, `/` 200 in 0.17s — worth keeping as the last confirmed-good
  latency reading, since this session hasn't been able to check since.

## Needs Alijah
1. **Network policy, still blocking, now 5 days**: this session's egress
   proxy has rejected `dawnpipe.com` on every shift since 2026-08-25 (403
   on CONNECT, confirmed via `curl -sS $HTTPS_PROXY/__agentproxy/status`
   → `recentRelayFailures`, not a flaky timeout). Every health check this
   week has been dead weight. If daily health checks matter, `dawnpipe.com`
   needs adding to whatever egress allowlist governs this session.
2. Merge `keeper/2026-08-25-atomic-rep-delete` when convenient — still
   small, single-purpose, still merges clean, tests pass, four days open.
3. When you have a minute: the two `ops/` directories (`main`'s vs.
   `keeper-ops`'s) have been diverging since 2026-08-25 and nobody's
   reconciled them. Not urgent, but worth a decision on which one's
   authoritative before it gets confusing.

One branch to review/merge this shift: `keeper/2026-08-29-media-streams-design`
(design doc only, no code — safe to merge any time, or just read it in
place on the branch).
