# Keeper shift — 2026-09-02

## Site status: unverified (egress blocked, day 9)

Same session-network-policy 403 as every prior shift since 08-25 — the
agent proxy rejects the CONNECT to `dawnpipe.com:443` outright (confirmed
via `$HTTPS_PROXY/__agentproxy/status`, `recentRelayFailures` shows two
`connect_rejected` / gateway-403 entries for this run). Not a site issue;
this session simply cannot reach production to check. No fix available
from inside the session — this needs a network-policy change on the
environment config, not more retries.

## Sanity: clean

`node --check server.js` and every file in `lib/` — all pass. No new
commits landed on `main` since `c577195` (08-26), so nothing new to
sanity-check either.

## Shipped today

**`keeper/2026-09-02-db-write-compact`** — `lib/db.js`'s `write()` was
pretty-printing every JSON store write (`JSON.stringify(d, null, 2)`).
Nothing reads these files by eye; the formatting was pure CPU and bytes on
the hottest write path (`saveCall` runs on every conversational turn of
every live call). Dropped it — compact JSON now, ~40% smaller writes,
zero behavior change. `node --check` clean, `node test-reps.js` 37/37
still passing.

## Needs Alijah — in priority order

1. **Merge `audit/2026-08-26-voice-status-fallback-billing` — this is
   actively costing customers money today.** Found while going through
   branches not tracked in this queue: a *separate* code-audit session ran
   08-26 and caught a real billing bug — `/voice/status`'s fallback path
   (runs whenever the in-memory call record is gone, i.e. a deploy landed
   mid-call — routine, since Render auto-deploys `main` and this shift
   pushes daily) billed the full call duration with no check for whether
   the caller ever spoke or the spam filter flagged it. The live path
   already refuses to bill those calls. Net effect: the same robocall is
   free or billed depending only on whether a deploy happened to be
   mid-flight — a few calls a week overbilled against Dawnpipe's own
   stated rule. Fix is written, tested, and has sat mergeable for 7 days.
   Re-verified today it still merges clean against current `main`:
   ```
   git fetch origin
   git checkout main && git pull
   git merge --no-ff origin/audit/2026-08-26-voice-status-fallback-billing
   git push origin main
   ```
   Full writeup incl. the one trade-off worth reading before merging (a
   call that dies before the first agent reply now goes unbilled by at
   most 1 minute — deliberately chosen over ever overbilling) is
   `ops/audit/2026-08-26.md` on branch `keeper-audit`, finding 1.

2. **Merge the 6 outstanding `keeper/*` branches.** Oldest is
   `keeper/2026-08-25-atomic-rep-delete` — 9 days unmerged, fixes a live
   raw-`writeFileSync` bypass of the atomic write path on the commission
   ledger. The rest (`2026-08-27` ConversationRelay design,
   `2026-08-28` activation-funnel + COST.md sync, `2026-08-29` Media
   Streams design, `2026-08-30` prune-reset-tokens,
   `2026-08-31` reconcile-stale-ops-docs, and today's
   `2026-09-02-db-write-compact`) are all still sitting too. All verified
   mergeable against current `main`. `git log main..keeper/<branch> --stat`
   to review each, then merge whichever you're comfortable with.

3. **Is `keeper-audit` a standing job you're running separately?** Its
   branch carries `ops/audit/2026-08-26.md`, a thorough independent review
   of the voice call state machine (5 findings, 1 already fixed on the
   branch above) that this shift only discovered today, 7 days after the
   fact — it isn't tracked in `ops/IMPROVEMENT-QUEUE.md` or referenced from
   `ops/KEEPER-LOG.md`. If it's recurring, worth telling this shift so its
   findings get folded into the queue as they land instead of going stale
   unseen. Folded its other 4 findings + suggestions into queue items 12–13
   today (idempotent voice billing, per-account `calls.json` retention, a
   dead-code policy conflict on the do-not-call path, an undercounted
   transferred-call cost). Also noticed branch `research-competitors`
   (pricing/positioning baseline incl. a note on Zoom's new $29.99/mo AI
   receptionist) sitting unmerged — unrelated to ops, flagging only so
   it isn't lost.

No customer-facing changes proposed today (email verification and the
2-question onboarding flow, queue items 2 and 8, remain open and still
need a product decision from you before implementation).
