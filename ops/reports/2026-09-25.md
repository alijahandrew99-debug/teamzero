# Keeper report — 2026-09-25

**Site health: unverifiable this session** (same gateway-level 403 on CONNECT to
`dawnpipe.com:443`, `connect_rejected` / "organization policy", reconfirmed via
the proxy status endpoint — 32nd consecutive day since first hit 08-25.
Unchanged root cause: this session's environment egress allowlist doesn't
include `dawnpipe.com` (pinned exactly 09-15). Not evidence of an outage —
just can't be checked from here.

**Sanity: clean.** `node --check` on `server.js` and all 28 `lib/*.js` files
against fresh `main` (`facb991`). Suites not re-run today (no code shipped
that touches them; last full run 09-24 was 96/96).

## What happened today

`main` moved for the first time since 09-16 (`a70c1ec` → `facb991`, one
commit): the `/signup` page's hardcoded "Plans from $49/mo" subtitle (stale —
cheapest real plan is Starter $99) is now `plans.fromPrice()`, substituted on
the page render, the referral path, and every POST error re-render. Audited
it the way this shift audits every new `main` commit: read the full diff,
confirmed `fromPrice()` returns a plain numeric string (no injection surface)
and that all four render paths (`GET /signup`, and all three `POST /signup`
error branches via the new shared `errSignup()` helper) substitute it — no
raw `__FROM_PRICE__` leak on any path. No bug found; nothing to fix.

Because `main` moved, re-verified (not just re-asserted) that the backlog's
top-priority branches still merge clean against the new base, since that's
genuinely new information rather than the repeated no-op checks flagged as
overhead on quiet days. Real `git merge --no-commit --no-ff` in an isolated
worktree: `audit/2026-09-23-send-lock-duplicate` +
`audit/2026-09-23-readbody-utf8` + `audit/2026-09-16-smtp-crlf-injection` +
`audit/2026-09-16-esc-quotes`, all four together, against `facb991` — merges
clean, `node --check` clean on `server.js`/`lib/smtp.js`, `test-sending-mode.js`
9/9 and `test-spam.js` 18/18 pass on the merged tree. Nothing broke.

No new `keeper-audit` run since 09-23 (last independent audit lens: (a),
server.js request/webhook handlers). No new branches beyond the 23 already
tracked (14 `keeper/*` + 9 `audit/*`).

Friday `COST.md` re-read: no discrepancies, figures still match shipped code
(prompt-caching item still correctly marked Done). Repeated below per the
weekly cadence.

No branch shipped today — the one new commit was correct as-is, and
re-verifying the standing branches against the moved `main` was this shift's
one substantive activity.

## Needs Alijah (≤3, exact actions)

1. **Merge backlog is the whole bottleneck — 23 branches, zero merged, five
   are live billing/security/deliverability bugs.** Top four, all
   independently re-verified clean today against current `main`:
   - `audit/2026-09-23-send-lock-duplicate` — High: a second click on a send
     job past ~6 emails force-expires it and double-sends the remainder, live
     since 09-15.
   - `audit/2026-09-23-readbody-utf8` — High: multi-byte UTF-8 split across a
     TCP chunk can corrupt the Stripe webhook signature check, silently
     dropping a genuine payment event.
   - `audit/2026-09-16-smtp-crlf-injection` — Critical: unvalidated lead
     emails let a CRLF forge SMTP commands through Dawnpipe's own sending
     mailbox.
   - `audit/2026-09-16-esc-quotes` — Medium: unescaped quotes in an HTML
     attribute, one path crosses into the owner's own admin session.
   All four merge cleanly together, right now, in one pass:
   `git fetch origin && git checkout main && git pull && git merge --no-ff origin/audit/2026-09-23-send-lock-duplicate origin/audit/2026-09-23-readbody-utf8 origin/audit/2026-09-16-smtp-crlf-injection origin/audit/2026-09-16-esc-quotes`
   (locally, then push). This has been the top ask for over a week and is
   costing real money and customer trust every day it sits.
2. **Standing weekly reminder (Friday):** check `/api/admin/spend` →
   `diagnostics` (owner login) for which speech-recognition model is billed.
   If it shows a legacy model (~$0.035–0.040/use vs. the $0.02 default), set
   `VOICE_STT=default` in Render — worth ~$45–60 per 1,000 minutes, one env
   var, zero code change. Last raised 09-18.
3. **This session's own network policy blocks `dawnpipe.com` egress** — a
   32-day-old, purely environmental issue (not a site problem) that's
   prevented direct health/SEO-monitor checks (`ops/seo-check.js`) this whole
   time. Worth deciding whether that's worth fixing on the environment side,
   or just accepted as a standing limitation of this shift.
