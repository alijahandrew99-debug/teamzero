# Keeper Log

One line per shift.

- 2026-08-25: Site health unverified (session network policy blocked egress to dawnpipe.com; not confirmed down). GitHub push access was denied for most of the shift (App/connector issue), restored mid-shift by Alijah. Shipped `keeper/2026-08-25-atomic-rep-delete`: `reps.json` deletes now go through the atomic write path (were bypassing it). Seeded `ops/IMPROVEMENT-QUEUE.md`.
- 2026-08-26: Site health unverified again (same egress 403, two days running — flagged for Alijah). Sanity clean. Discovered `main` has grown its own separate, diverging `ops/` queue (commit `ad5e278`) — flagged, not touched. No code shipped: audited queue items 1/3/4, found 3 and 4 already resolved in code (closed both, corrected a stale COST.md cost figure), confirmed item 1's pending branch still merges clean. All changes are docs, committed straight to `keeper-ops`.
- 2026-08-27: Site health unverified again (same egress 403, three days running — flagged for Alijah again). Sanity clean. Shipped `keeper/2026-08-27-conversationrelay-design`: `ops/design/conversationrelay-migration.md`, queue item 5, grounded in the actual `lib/voice.js`/`/voice/*` code — maps what ports to ConversationRelay cleanly vs. what needs a spike first (partial speculation, noise-driven barge-in, hints, machine detection, recording, auth), recommends a half-day spike before implementation. Verified queue item 2 (email verification) still fully open; proposed 3 gating options in the report rather than implementing (customer-facing).
- 2026-08-28: Site health unverified again (same egress 403, confirmed via proxy status endpoint as a genuine policy denial — four days running, flagged for Alijah with the diagnostic command). Sanity clean, tests pass. Shipped `keeper/2026-08-28-activation-funnel`: queue item 7, `db.trackFunnel()`/`getFunnelSummary()` into a new `funnel.json`, hooked at signup/profile-save/first-lead/first-send/upgrade, owner-only `GET /api/admin/funnel`. Also shipped `keeper/2026-08-28-cost-md-sync`: ported the 08-26 prompt-caching COST.md correction from `keeper-ops`'s shadow copy to `main`, which it never reached. Friday COST.md re-read: nothing new besides the sync above; repeated the standing `VOICE_STT` spend-check reminder. Queue items 1, 2, 6 re-confirmed still open/unactioned; the divergent-`ops/`-states issue from 08-26 is still unreconciled.
- 2026-08-29: Site health unverified again (same egress 403, five days running — flagged for Alijah again). Sanity clean, tests pass. Shipped `keeper/2026-08-29-media-streams-design`: `ops/design/media-streams-deepgram-polly-design.md`, queue item 6 (arch D), grounded in `lib/voice.js` and item 5's arch-C doc — scopes the WebSocket-server/Deepgram/Polly-SigV4 pieces D needs beyond C, checks each against the zero-npm-dependency rule (all feasible, the WS server is the hard one), lists what has to be rebuilt vs. ported. Recommends holding implementation until arch C's own spike lands. Re-verified queue items 1 (branch still merges clean, no new bypass sites, still unmerged) and 2 (still fully open) unchanged. Divergent-`ops/`-states issue still unreconciled, flagged again.
- 2026-08-30: Site health unverified again (same egress 403, six days running — a full week now, flagged again more urgently). Sanity clean, tests pass. Shipped `keeper/2026-08-30-prune-reset-tokens`: `db.pruneResets()` mirrors the existing `pruneSessions()` sweep so `resets.json` (password-reset tokens) can't grow unbounded from expired-but-never-used tokens; wired into the same 10-min interval, smoke-tested against an isolated `DATA_DIR`. Re-verified queue item 1's fix branch (`keeper/2026-08-25-atomic-rep-delete`) still merges clean and is still unmerged after a week, while the bug it fixes (`lib/reps.js:146` raw `writeFileSync` on the commission ledger) remains live on `main` — flagged with the exact merge command. Divergent-`ops/`-states issue on `main` still unreconciled, flagged again.
- 2026-08-31: Site health unverified again (same egress 403, seven days running — flagged again). Sanity clean; no new commits on `main` since 08-26 so nothing new to audit. Shipped `keeper/2026-08-31-reconcile-stale-ops-docs`: removed the stale duplicate `ops/IMPROVEMENT-QUEUE.md`/`KEEPER-LOG.md` that have sat unreconciled on `main` since 08-26 (superseded by `keeper-ops`), added `ops/README.md` pointing here, kept `ops/seo-check.js`. Discovered `ops/seo-check.js` is a real daily SEO/trust-signal monitor (added 08-25, never run by this shift before) — ran it, got the same 403 as the health check, confirming the block is session-side not site-side. Re-verified `keeper/2026-08-25-atomic-rep-delete` still merges clean, still unmerged, now a full week old — flagged as pure overhead to keep re-auditing; recommend Alijah just merge the 6 outstanding `keeper/*` branches.
- 2026-09-01: Site health unverified again (same egress 403 on both curl and WebFetch, eight days running — flagged again). Sanity clean, tests pass (37/37). No new commits on `main` since 08-26, so nothing new to audit and nothing new to ship — every code-workable queue item is shipped, closed, waiting purely on a merge, or correctly held as a customer-facing proposal. Re-verified `keeper/2026-08-25-atomic-rep-delete` with an actual `git merge --no-commit` (not just `git merge-tree`, which today gave a false-positive "changed in both" on two files that the real merge auto-resolves) — still clean, still unmerged, eight days now, still the top ask for Alijah. No branch shipped today; this was a genuinely quiet, verification-only shift.
- 2026-09-02: Site health unverified again (same egress 403 confirmed via proxy status, nine days running). Sanity clean; no new commits on `main` since 08-26. Shipped `keeper/2026-09-02-db-write-compact`: dropped pretty-printing from `db.js`'s `write()` (`JSON.stringify(d, null, 2)` -> compact), ~40% smaller writes on the hottest path (`saveCall`, every turn of every live call), zero behavior change, 37/37 tests pass. Bigger finding: discovered 3 branches this shift never knew about — `audit/2026-08-26-voice-status-fallback-billing`, `keeper-audit`, `research-competitors` — from an independent code-audit session that ran 08-26 and was never tracked in this queue. It found and already fixed a real billing bug: `/voice/status`'s fallback path (taken whenever a deploy lands mid-call, which is routine) billed robocalls/pocket-dials/spam calls in full while the live path correctly refuses to bill them — same call, free or billed, purely on deploy timing. Fix branch re-verified today, still merges clean, 7 days unmerged, now the top ask ahead of item 1. Folded the audit's other 4 findings into queue items 12-13 (voice-billing idempotency, per-account `calls.json` retention, a DNC dead-code policy conflict, undercounted transfer-leg cost) and flagged for Alijah whether `keeper-audit` is a standing separate job this shift should be watching for.
- 2026-09-03: Site health still unverifiable — now a genuine org-level egress policy denial (403 on CONNECT to dawnpipe.com, confirmed via the proxy status endpoint), ten days running, worth Alijah's attention on its own since it's no longer a transient block. Sanity clean, tests pass (37/37 + 18/18 spam). No new commits on `main` since 08-26. Shipped `keeper/2026-09-03-billing-idempotency`: queue item 12's first bullet — added a `billedMin` stamp so a duplicate `/voice/status` callback can't double-bill a call's minutes, scoped to the live-call path only (left the restart-recovery fallback alone since item 11's pending audit branch already rewrites those exact lines). While scoping that, tested the audit branch's actual mergeability with a real `git merge --no-commit --no-ff` in an isolated worktree rather than trusting the raw branch-vs-main diff, which looked like it would revert `c577195`'s password-reset fix (it was cut before that commit landed) — the real merge auto-resolves clean and both fixes survive; flagging this because a future shift trusting the diff-only view could wrongly sound an alarm. Unmerged-branch count is now 8 `keeper/*` branches plus the audit branch, several 7-10+ days old and independently re-verified as clean — that backlog, not remaining Keeper work, is the actual bottleneck now.
- 2026-09-04: Site health still unverifiable (same egress 403 confirmed via proxy status, 11 days running). Sanity clean, tests pass (37/37). No new commits on `main` since 08-26. Shipped `keeper/2026-09-04-unique-tmp-write`: `db.js`'s `write()` now uses a unique temp filename per call instead of a fixed `f+'.tmp'`, closing a silent-collection-loss risk the 2026-09-02 audit flagged but couldn't confirm the trigger for (whether Render deploys ever overlap) — fixed defensively rather than waiting on that answer. Folded the 2026-09-02 audit run's findings into the queue as items 14-16: a new URGENT metering bug (`audit/2026-09-02-consume-stale-snapshot` — night-shift concurrent jobs silently erase most of a run's metered leads/voice minutes, reproduced 21-metered-1-recorded), extending per-account retention to `smsThreads` (deferred, needs an ordering fix first), and a ten-minute Render-dashboard question for Alijah. Merge backlog is now 12 small branches deep, two of them active billing/metering-correctness bugs.
- 2026-09-05: Site health still unverifiable — confirmed via the proxy status endpoint as a genuine gateway-level 403 on CONNECT to `dawnpipe.com:443` (org egress policy denial, not a site error), 12 days running. Sanity clean: `node --check` on `server.js` + every `lib/*.js`, and all four suites pass (37 reps + 18 spam + 17 voiceprofiles + 15 assistant = 87/87). No new commits on `main` since 08-26, so yesterday's mergeability verifications for the standing branches stand unchanged — not re-run today. Shipped `keeper/2026-09-05-per-account-retention`: queue item 15, cut fresh off `main`. Added `capPerAccountOldestFirst()` (walks backward, keeps the newest N per account, restores oldest-first order) and applied it to both `saveCall`'s `calls.json` write (was a global `slice(-3000)`) and `saveThread`'s `smsThreads.json` write (was `slice(-2000)`), closing the "one busy tenant evicts everyone else's history, and by creation order rather than recency" bug the 2026-08-26/09-02 audits flagged but held off shipping. `node --check` clean, `test-reps`/`test-spam` unaffected, plus a 6-assertion smoke test against three isolated `DATA_DIR`s proving a quiet tenant survives a busy tenant's flood and a stale-but-recently-updated row survives over an untouched-but-newer one. Also hit today's own `ops/` merge-conflict recurrence: merging `origin/main` into `keeper-ops` conflicted (add/add) on `IMPROVEMENT-QUEUE.md`/`KEEPER-LOG.md` because `main`'s stale duplicate from `ad5e278` (08-26) is still there — `keeper/2026-08-31-reconcile-stale-ops-docs` (the branch that already fixes this) is still unmerged, so the same conflict will recur on every future `main`-merge into `keeper-ops` until that branch lands; resolved locally by keeping `keeper-ops`'s own docs. Merge backlog is now 13 small branches deep, unchanged in substance from yesterday; the reconcile-stale-ops-docs branch is worth prioritizing since it's now actively causing shift friction, not just clutter.
- 2026-09-07: Site health still unverifiable — same gateway-level 403 on CONNECT to `dawnpipe.com:443`, confirmed via the proxy status endpoint, 13 days running, unchanged from yesterday. Sanity clean: `node --check` on `server.js` + all 28 `lib/*.js` files against fresh `main`. No new commits on `main` since 08-26 (`git rev-list origin/main ^c577195 --count` = 0, 12 days), no new `keeper-audit` run since 09-02, no new branches beyond the known 14 `keeper/*` + 2 `audit/*` backlog. Nothing shipped today: every code-actionable queue item is already shipped, closed, correctly held as a customer-facing proposal, or waiting purely on a merge, and re-verifying the same unchanged branches against the same unchanged `main` again would be motion without new information (exactly the "pure overhead" pattern flagged 09-01/09-03). Genuinely quiet, report-only shift. Merge backlog remains 14 `keeper/*` + 2 `audit/*` branches, oldest 13 days old, two of them live billing/metering-correctness bugs — that backlog is the top ask again today.
- 2026-09-06: Site health still unverifiable — same gateway-level 403 on CONNECT to `dawnpipe.com:443` (org egress policy denial), confirmed via the proxy status endpoint, 14 days running. Sanity clean: `node --check` on `server.js` + all 28 `lib/*.js` files (run against a fresh `main` checkout, not `keeper-ops` — see the discovery below); `test-reps.js` 37/37 (the only suite in this session's working tree; `test-spam`/`test-voiceprofiles`/`test-assistant` exist on `main` but weren't re-run today). No new commits on `main` since 08-26 — still `c577195`, still 13 `keeper/*`/`audit/*` branches unmerged, all re-fetched and none have landed. Shipped `keeper/2026-09-06-transfer-leg-cost`: queue item 12's fourth bullet — `estimateCost()` was missing a transferred call's `<Dial>`-leg cost (Twilio bills that leg separately, at the outbound rate). `/voice/dialback` now stamps `DialCallDuration` on the call row when a transfer connects; `/voice/status` reads it back into a new `transferSec` param, which is a no-op at 0 so every non-transferred call is unaffected. `node --check` clean, `test-reps.js` 37/37, plus a direct smoke test of `estimateCost()` confirming the added cost is exactly `ceil(transferSec/60) * RATE_VOICE_MIN_OUT`. Merge backlog is now 14 branches deep. Separate discovery while working today: `keeper-ops` itself carries a stale, frozen copy of `server.js`/`lib/`/`views/` from its original branch-off point (`df0d31f`, 2026-08-24) — 471 lines behind current `main` on `server.js` alone, missing `test-spam.js`/`test-voiceprofiles.js`/`test-assistant.js` entirely. `git log keeper-ops` has zero merge commits, so it's never pulled `main` again since branching; this is almost certainly why every "merge `origin/main` into `keeper-ops`" attempt in recent shifts (09-05's log entry, item 9's history) hit conflicts — the branch has drifted far beyond just the two duplicate `ops/` docs. Not a production risk (`keeper-ops` never merges to `main`, and this shift's own sanity checks and fix branches are correctly run against fresh `main` checkouts, not this stale copy) but worth a cleanup pass — e.g. resetting `keeper-ops`'s non-`ops/` paths to match `main`, or moving to a true orphan branch with only `ops/` tracked — next time someone is touching ops branch hygiene.
- 2026-09-08: Site health still unverifiable — same gateway-level 403 on CONNECT to `dawnpipe.com:443` (session egress policy denial), confirmed via the proxy status endpoint, 16 days running. Sanity clean against fresh `main` (`c577195`): `node --check` on `server.js` + all `lib/*.js`; all four suites pass, 87/87 (37 reps + 18 spam + 17 voiceprofiles + 15 assistant). No new commits on `main` since 08-26 (`git rev-list origin/main ^c577195 --count` = 0, 16 days) and no new `keeper-audit` findings since 09-02, so nothing new to audit or ship — every code-workable queue item is already shipped, closed, held as a customer-facing proposal, or waiting purely on a merge. Did not re-run mergeability checks on the unchanged standing branches again (already re-verified against this exact `main` commit multiple times this week; would be motion, not new information). Two new unrelated branches noticed in passing (not ops's to act on): `hiring-screener` (automated weekly rep-applicant screen, ran today, no applicants) and previously-flagged `research-competitors`. No branch shipped today; report-only shift. Merge backlog remains 14 `keeper/*` + 2 `audit/*` branches, oldest 17 days, two of them live billing/metering-correctness bugs — still the top ask.
- 2026-09-09: Site health still unverifiable — same gateway-level 403 on CONNECT to `dawnpipe.com:443`, confirmed via both direct curl and WebFetch and the proxy status endpoint (`recentRelayFailures`), 18 days running, still the longest-standing open item. Sanity clean against fresh `main` (`c577195`, still unchanged since 08-26 — `git rev-list origin/main ^c577195 --count` = 0, 18 days): `node --check` on `server.js` + all `lib/*.js` + `ops/seo-check.js`; all four suites pass, 87/87. No code shipped (no new commits on `main` to audit, and re-verifying the standing branches' mergeability again would be the same motion-without-new-information flagged 09-01/09-03/09-07/09-08). Instead closed out queue item 8 (onboarding: "2 questions -> AI drafts profile -> instant first leads"), which had sat as an unstarted proposal placeholder since the queue was seeded: traced `server.js`/`lib/agents.js`/`views/app.html` and found it's substantially already shipped, predating this queue — `POST /api/profile/autofill` (`agents.autofillFromWebsite`) drafts the whole profile plus a phone script from one pasted URL (or raw text for sites it can't fetch), wired to a real button in the profile UI, with 2 free tries before any card is entered. One real gap (leads need a separate manual "Find leads" click, not instant) proposed as a small follow-up in the queue rather than implemented, since auto-firing a prospect run is a customer-facing behavior change. No Friday COST.md re-read (Tuesday). Merge backlog remains 14 `keeper/*` + 2 `audit/*` branches, oldest 18 days, two of them live billing/metering-correctness bugs — still the top ask, unchanged.
- 2026-09-10: Site health unverifiable — same gateway-level 403 on CONNECT to `dawnpipe.com:443` (session egress policy denial), confirmed via curl + proxy status endpoint, 19 days running. Sanity clean against fresh `main` (`c577195`, still 0 new commits since 08-26 — 19 days): `node --check` on `server.js` + all `lib/*.js`; all four suites pass, 87/87. No branch shipped; instead triaged 3 new audit branches from an independent session dated 09-09 that hadn't been seen before — `audit/2026-09-09-spam-strike-decay` (real live bug: decayed-row check let auto-block permanently stop re-arming for a repeat robocaller after 30 quiet days, plus unbounded duplicate rows in `spamwatch.json`), `audit/2026-09-09-voice-webhook-log-flood` (real live bug: unauthenticated bad-signature path could be hammered to stall every live call via unthrottled full-file activity-log rewrites), `audit/2026-09-09-duplicate-caller-history` (perf-only: `/voice/incoming` called `callerHistory()` 3x, now once). All three verified today: merge clean individually and together (91/91 combined suite), folded into the queue as items 17-19, flagged as the new top merge priority ahead of the standing backlog since two are live security/availability bugs. Standing backlog otherwise unchanged: 12 `keeper/*` + 2 older `audit/*` branches, oldest 19 days old, still unmerged.
- 2026-09-11: Site health still unverifiable — same gateway-level 403 on CONNECT to `dawnpipe.com:443`, confirmed via curl + proxy status endpoint, 18 days running since first hit 08-25 (recomputed the day count precisely today; prior entries had drifted up by ~1). Sanity clean against fresh `main` (`c577195`, still 0 new commits since 08-26): `node --check` on `server.js` + all `lib/*.js` + `ops/seo-check.js`; all four suites pass, 87/87. No new branches beyond the 17 already tracked. Nothing shipped — every code-workable queue item is already shipped, closed, held as a customer-facing proposal, or waiting purely on a merge, and did not re-verify the standing branches' mergeability again (already confirmed against this exact `main` commit repeatedly this week). Friday COST.md re-read: no discrepancies found, nothing new to correct; repeated the standing `VOICE_STT` spend-check reminder (last explicit mention 08-28). Merge backlog unchanged: 12 `keeper/*` + 5 `audit/*` branches, oldest 18 days old, four of them live billing/security/availability bugs — still the top ask.
