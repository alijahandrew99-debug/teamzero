# Keeper Log

One line per shift.

- 2026-08-25: Site health unverified (session network policy blocked egress to dawnpipe.com; not confirmed down). GitHub push access was denied for most of the shift (App/connector issue), restored mid-shift by Alijah. Shipped `keeper/2026-08-25-atomic-rep-delete`: `reps.json` deletes now go through the atomic write path (were bypassing it). Seeded `ops/IMPROVEMENT-QUEUE.md`.
- 2026-08-26: Site health unverified again (same egress 403, two days running — flagged for Alijah). Sanity clean. Discovered `main` has grown its own separate, diverging `ops/` queue (commit `ad5e278`) — flagged, not touched. No code shipped: audited queue items 1/3/4, found 3 and 4 already resolved in code (closed both, corrected a stale COST.md cost figure), confirmed item 1's pending branch still merges clean. All changes are docs, committed straight to `keeper-ops`.
- 2026-08-27: Site health unverified again (same egress 403, three days running — flagged for Alijah again). Sanity clean. Shipped `keeper/2026-08-27-conversationrelay-design`: `ops/design/conversationrelay-migration.md`, queue item 5, grounded in the actual `lib/voice.js`/`/voice/*` code — maps what ports to ConversationRelay cleanly vs. what needs a spike first (partial speculation, noise-driven barge-in, hints, machine detection, recording, auth), recommends a half-day spike before implementation. Verified queue item 2 (email verification) still fully open; proposed 3 gating options in the report rather than implementing (customer-facing).
- 2026-08-28: Site health unverified again (same egress 403, confirmed via proxy status endpoint as a genuine policy denial — four days running, flagged for Alijah with the diagnostic command). Sanity clean, tests pass. Shipped `keeper/2026-08-28-activation-funnel`: queue item 7, `db.trackFunnel()`/`getFunnelSummary()` into a new `funnel.json`, hooked at signup/profile-save/first-lead/first-send/upgrade, owner-only `GET /api/admin/funnel`. Also shipped `keeper/2026-08-28-cost-md-sync`: ported the 08-26 prompt-caching COST.md correction from `keeper-ops`'s shadow copy to `main`, which it never reached. Friday COST.md re-read: nothing new besides the sync above; repeated the standing `VOICE_STT` spend-check reminder. Queue items 1, 2, 6 re-confirmed still open/unactioned; the divergent-`ops/`-states issue from 08-26 is still unreconciled.
- 2026-08-29: Site health unverified again (same egress 403, five days running — flagged for Alijah again). Sanity clean, tests pass. Shipped `keeper/2026-08-29-media-streams-design`: `ops/design/media-streams-deepgram-polly-design.md`, queue item 6 (arch D), grounded in `lib/voice.js` and item 5's arch-C doc — scopes the WebSocket-server/Deepgram/Polly-SigV4 pieces D needs beyond C, checks each against the zero-npm-dependency rule (all feasible, the WS server is the hard one), lists what has to be rebuilt vs. ported. Recommends holding implementation until arch C's own spike lands. Re-verified queue items 1 (branch still merges clean, no new bypass sites, still unmerged) and 2 (still fully open) unchanged. Divergent-`ops/`-states issue still unreconciled, flagged again.
